# Generated by `pnpm gen:types` (supabase gen types typescript) — its layout is
# the tool's, not ours, so don't format-lint it. Mirrors the root .prettierignore
# entry so a package-level `pnpm lint` skips it too.
src/database.types.ts
