# React Native / Metro do not fully understand pnpm's symlinked, isolated
# node_modules layout. Use a hoisted node-linker so the Expo app can resolve
# its (and React Native's) dependencies the way Metro expects.
node-linker=hoisted
